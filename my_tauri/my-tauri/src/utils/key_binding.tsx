const key_bindings: string[] = [];

const searchbar_show = 'Control+ALT+P';
const searchbar_hide = 'Control+ALT+H';
key_bindings.push(searchbar_show);
key_bindings.push(searchbar_hide);

export { key_bindings, searchbar_show, searchbar_hide };
